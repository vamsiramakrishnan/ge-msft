{
  "name": "@ge/graph-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Client-direct Microsoft Graph reads/search as the signed-in user (Plane B) -> ResolvedContext. No secrets; delegated token only.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@ge/contracts": "*",
    "@ge/content": "*",
    "zod": "^3.23.0"
  }
}
