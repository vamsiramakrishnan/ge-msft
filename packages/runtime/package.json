{
  "name": "@ge/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Surface-agnostic runtime: DocBridge/AuthClient interfaces + the assist loop (streamAssist <-> SessionContext <-> actuations). Reused by every bridge.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@ge/contracts": "*",
    "@ge/content": "*",
    "@ge/gemini-client": "*",
    "@ge/triggers": "*"
  }
}
