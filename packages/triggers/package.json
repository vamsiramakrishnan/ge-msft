{
  "name": "@ge/triggers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Event-driven trigger engine (Claude-Code-style hooks): host/estate/actuation lifecycle events -> matched triggers -> outcomes (continue/block/suggest/automate).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@ge/contracts": "*"
  }
}
