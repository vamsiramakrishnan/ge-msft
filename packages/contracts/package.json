{
  "name": "@ge/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types + Zod schemas (gateway<->client boundary)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
