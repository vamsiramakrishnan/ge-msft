name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  build-test:
    name: typecheck · lint · test · build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: typecheck
        run: npm run typecheck
      - name: lint
        run: npm run lint
      - name: test
        run: npm run test
      - name: build
        run: npm run build
      # ADR-0008 §4 drift gate: the committed language manifest must equal the emitter output.
      - name: language-manifest is not stale
        run: |
          npm run emit:language
          git diff --exit-code skill/m365-surface-commander/scripts/m365-cli-1.0.json \
            || { echo "::error::m365-cli-1.0.json is stale — run 'npm run emit:language' and commit."; exit 1; }

  manifests:
    name: manifest generate · validate · lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      # Placeholder-leakage and version-drift gate: generate the development-profile manifests
      # from source and run the release validator over them, so a PR that bakes REPLACE_ tokens,
      # example.com hosts, unresolved templates, or a stale version fails here.
      - name: generate development manifests
        run: npm run manifests:generate -- --profile development
      - name: validate development manifests
        run: npm run manifests:validate -- --profile development
      # Lint the committed authoritative manifests (manifests/*) with the contracts test suite.
      - name: manifest lint tests
        run: npm run validate:manifests

  skill-python:
    name: skill parsers · parity · eval
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: skill/requirements.txt
      # test_tooling.py loads create_skill.py, which imports requests at module level — the
      # tooling deps must be installed or the job dies at import before any test runs.
      - name: install skill tooling deps
        run: pip install -r skill/requirements.txt
      - name: parse_commands self-test
        run: python3 skill/m365-surface-commander/scripts/parse_commands.py --self-test
      - name: surface_cli self-test
        run: python3 skill/m365-surface-commander/scripts/surface_cli.py --self-test
      - name: planner + corpus parity
        working-directory: skill
        run: |
          python3 parity_test.py
          python3 parity_corpus_test.py
      # The updater tests validate against the real bundle zips (dry-run plans, resumable
      # upload framing), so both bundles must be built first — they are build artifacts,
      # not committed files.
      - name: build skill bundle zips
        working-directory: skill
        run: |
          ./build_zip.sh m365-surface-commander
          ./build_zip.sh m365-command-planner
      - name: tooling unit tests
        working-directory: skill
        run: python3 test_tooling.py
      - name: deterministic eval gate (recall / false-positives)
        working-directory: skill
        run: python3 eval/eval_harness.py
